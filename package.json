{
  "name": "passkey-ex",
  "scripts": {
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down",
    "server:rust": "cd server && cargo watch -x run",
    "server:node": "cd node_server && pnpm serve",
    "client:dev": "cd client && pnpm run dev"
  }
}
